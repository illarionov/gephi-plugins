plugins {
    id('groovy-gradle-plugin')
}

repositories {
    gradlePluginPortal()
    maven {
        url 'https://repo.gradle.org/gradle/libs-releases'
    }
}

dependencies {
    implementation 'cz.kubacki.gradle.plugins:gradle-nbm-plugin'
}
